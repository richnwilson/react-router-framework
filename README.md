# Router-Framework

Installed using `npx create-vite@latest router-framework` and picking `react-router` during install - this install Typescript and Tailwind
A modern, production-ready template for building full-stack React applications using React Router.

## Functionality

This includes exposure to the following:

- React Router
- Typescript
